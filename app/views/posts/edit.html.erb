<div class="row">
	<div class="twelve columns post-form">
		<%= image_tag @post.image.url(:medium) %>
		<%= simple_form_for @post do |f| %>
		  	<%= f.input :image %>
		  	<%= f.input :caption %>
		  	<%= f.button :submit %>
  		<% end %>
  	</div>
 </div>